const numeros = [14, 16, 20, 42];

const somaTotal = numeros.reduce((acumulador, atual) => acumulador + atual, 0);


const somaTotal = numeros.reduce((acumulador, atual) => acumulador + atual, 0);

console.log(somaTotal); 
